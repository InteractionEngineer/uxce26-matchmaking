services:
  matchmaking:
    build: .
    container_name: uxcamp-matchmaking
    ports:
      - "3000:3000"
    volumes:
      - uxcamp-data:/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DATA_DIR=/data

volumes:
  uxcamp-data:
